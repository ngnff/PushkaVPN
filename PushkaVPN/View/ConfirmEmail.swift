import SwiftUI

func ConfirmEmail()
{
    let alert = UIAlertController(title: "", message: "Письмо с подтверждением отправленно на почту", preferredStyle: .alert)
    
    let okButton = UIAlertAction(title: "Ok", style: .default) { (_) in
        <#code#>
    }
}
